//@ts-check
import './-np-globals';

import * as constructors from './np-constructors';
import * as grammar from './np-grammar';
import * as random from './np-random';

export const modules = {
  constructors,
  grammar,
  random,
};